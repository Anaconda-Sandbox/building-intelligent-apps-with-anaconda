name: foundation
channels:
  - defaults
dependencies:
  # Core Python
  - python>=3.10
  - pip

  # LLM & AI Frameworks
  - langchain
  - llama-index
  - pydantic>=2.0

  # MCP (Model Context Protocol)
  - mcp
  - typer  # Needed for mcp CLI
  - python-dotenv  # Needed for mcp CLI
  - fastmcp

  # Agent & Workflow Frameworks
  - langgraph

  # Async & HTTP utilities
  - aiohttp
  - python-dotenv

  # Panel App
  - panel
  - panel-material-ui
