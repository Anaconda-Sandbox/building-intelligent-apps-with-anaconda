name: deployment-inference
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
 
  # ── Jupyter ───────────────────────────────────────────────────────────────────
  - ipykernel
  - jupyterlab
 
  # ── Data pipeline ─────────────────────────────────────────────────────────────
  # Inherited from Modules 01-03 via inference_client.py and agent imports.
  - polars>=1.0
  - numpy>=1.26
  - pydantic>=2.0
 
  # ── Inference stack ───────────────────────────────────────────────────────────
  # All three confirmed on conda-forge — no pip section required.
  #
  # openai:     OpenAI-compatible client — the portable contract for AI Navigator,
  #             vLLM, and Anaconda Platform Model Servers (inference_client.py)
  # anthropic:  Anthropic SDK — Claude Haiku fallback path
  # langgraph:  Agent framework from Module 03, imported by flow steps
  - openai>=1.30
  - anthropic
  - langgraph

  # ── Utilities ─────────────────────────────────────────────────────────────────
  - python-dotenv   # used in setup patterns across the curriculum